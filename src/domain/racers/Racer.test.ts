import { Racer } from './Racer';

describe('Racer', () => {
  it('REMOVE ME LATER', () => {
    const aName = 'patate';
    const actual = new Racer({ name: aName });

    expect(actual.name).toBe(aName);
  });
});
